"use strict";

import openLink from "./busi/openLink";
import getLocation from "./device/getLocation";
const busi =  {
    openLink
};
const device = {
    getLocation
};

export default { busi, device };
